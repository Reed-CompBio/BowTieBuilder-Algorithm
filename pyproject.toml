[project]
name = "bowtiebuilder-algorithm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "networkx>=3.5",
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]
